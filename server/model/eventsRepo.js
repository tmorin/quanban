var Q = require('q'),
    events = require('../../server/model/events'),
    Event = events.Event;

function findById(project, eventId) {
}

function save(project, eventId) {
}

function remove(project, eventId) {
}

function listAll(project) {
}

function listFrom(project, from) {
}

module.exports = {
    findById: findById,
    save: save,
    remove: remove,
    listAll: listAll,
    listFrom: listFrom
}
