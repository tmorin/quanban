var Q = require('q'),
    events = require('../../server/model/events'),
    Event = events.Event;

function findById(project, commentId) {
}

function save(project, commentId) {
}

function remove(project, commentId) {
}

function listAll(project) {
}

function listFrom(project, from) {
}

module.exports = {
    findById: findById,
    save: save,
    remove: remove,
    listAll: listAll,
    listFrom: listFrom
}
